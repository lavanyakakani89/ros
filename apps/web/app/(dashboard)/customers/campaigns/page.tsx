import { CustomerCampaigns } from "@/components/customers/customer-campaigns";

export const metadata = { title: "Customer Campaigns — RetailOS" };

export default function CustomerCampaignsPage() {
  return <CustomerCampaigns />;
}
