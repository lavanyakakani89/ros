import { KdsClient } from "@/components/restaurant/kds-client";

export const metadata = { title: "Kitchen Display — BizBil" };

export default function RestaurantKdsPage() {
  return <KdsClient />;
}
